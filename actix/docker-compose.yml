version: "3.7"
services:
#  database:
#    image: postgres
#    restart: always
#    env_file:
#      - .env
#    ports:
#      - "5432:5432"
#    volumes:
#      - data:/var/lib/postgresql/data
  server:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    networks:
      - network1
    ports:
      - "8780:8780"
volumes:
  data:
networks:
  network1:
    name: pgnetwork
    external: true